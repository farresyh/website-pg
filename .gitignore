# Secrets — never commit an env file at any level
.env
*.env
!.env.example

# OS / editor noise
.DS_Store
Thumbs.db
.idea
.vscode
.zed
.cursor/

# Ruflo / claude-flow local state (not project source)
.claude-flow/
.swarm/
ruvector.db
