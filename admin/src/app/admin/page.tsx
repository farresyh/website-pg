/**
 * TODO: DASH-1..DASH-6 (docs/prd.md §6.2) — KPI cards, System Health,
 * Conversion Funnel, Top Games, Hourly Activity. Placeholder until the
 * corresponding Laravel endpoints exist.
 */
export default function AdminDashboardPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold">Dashboard</h1>
      <p className="mt-2 text-sm text-black/60 dark:text-white/60">
        Placeholder — wire up once Laravel API endpoints for DASH-1..DASH-6
        exist.
      </p>
    </div>
  );
}
