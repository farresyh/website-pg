/**
 * TODO: MUI-1 (docs/prd.md §6.20) — supplier status cards, quick actions,
 * recent API calls, circuit-breaker state. Placeholder until the Adapter
 * layer (§6.21, ADAPT-1..3) exists in Laravel.
 */
export default function MiddlewareDashboardPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold">Middleware Dashboard</h1>
      <p className="mt-2 text-sm text-black/60 dark:text-white/60">
        Placeholder — wire up once the Laravel Adapter layer (ADAPT-1..3)
        exists.
      </p>
    </div>
  );
}
